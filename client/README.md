# LinkFolio Polygon Client

TanStack Start client for LinkFolio on Polygon Amoy.

## Environment

Create a local `.env` with:

```sh
VITE_REOWN_PROJECT_ID=
VITE_SUBGRAPH_API_URL=
PINATA_JWT=
VITE_PINATA_GATEWAY_URL=
```

## Commands

```sh
npm install
npm run dev
npm run build
```
